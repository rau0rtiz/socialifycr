Ocultar el campo "Duración aproximada (minutos)" en `src/components/ventas/ProductFormDialog.tsx` cuando `isTissue` sea true (retail no necesita duración). Una sola edición envolviendo el bloque `{/* Duration */}` con `{!isTissue && (...)}`.